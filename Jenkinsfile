pipeline {
    agent any

    stages {
       stage('Checkout') {
            steps {
              
            }
        }
        stage('Build') {
            steps {
              sh "echo Build"
            }
        }
      stage('Deploy') {
            steps {
              sh "echo Deploy"
            }
        }
    }
}
